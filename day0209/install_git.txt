【GitバッシュでPython外部ライブラリをインストールする】

# 先ずpipをインストール
py -m pip install -U pip

# numpyをインストール
py -m pip install numpy

# matplotlibをインストール
py -m pip install matplotlib

# requestsをインストール
py -m pip install requests